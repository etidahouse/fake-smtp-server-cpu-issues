{
  "name": "mail-test",
  "scripts": {
    "deps": "yarn --check-files --frozen-lockfile",
    "dev": "jest --watch",
    "test": "yarn jest --coverage --forceExit",
    "reset": "rm -rf build yarn.lock package-lock.json node_modules/* && yarn && jest --clearCache",
    "up": "yarn upgrade-interactive --latest"
  },
  "type": "module",
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0",
    "node-fetch": "2.6.9",
    "ts-jest": "^29.1.1"
  },
  "dependencies": {
    "@types/nodemailer": "6.4.9",
    "nodemailer": "6.9.4"
  }
}
